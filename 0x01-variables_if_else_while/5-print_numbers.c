#include <stdio.h>

/**
 * main - starting point of code running
 * prints all single digit number of base 10
 * Return: 0 is successful
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
